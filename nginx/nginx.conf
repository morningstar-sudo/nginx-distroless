worker_processes auto;
pid /tmp/nginx.pid;
error_log /dev/stderr warn;

events {
    worker_connections 1024;
}

http {
    include /etc/nginx/common.conf;
}

# Extension point: downstream mail{} proxy configs (top-level context).
include /etc/nginx/mail.d/*.conf;
