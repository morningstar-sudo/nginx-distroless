# Placeholder for downstream stream proxy configs (top-level stream{} context).
# Downstream images: COPY your-stream.conf /etc/nginx/stream.d/
# Example:
#   stream {
#       server {
#           listen 5432;
#           proxy_pass db.internal:5432;
#       }
#   }
